{
  "name": "test-iterative-fixture",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "ci": "echo 'iterative ci test' && exit 0",
    "ci:admin": "echo 'iterative ci:admin should not be called' && exit 1"
  },
  "dependencies": {
    "lodash": "^4.17.19",
    "semver": "^7.3.0"
  },
  "devDependencies": {
    "vitest": "^0.28.0"
  },
  "optionalDependencies": {
    "sharp": "^0.31.0"
  }
}
