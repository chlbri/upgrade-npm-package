import type { StateValue } from '@bemedev/app-ts/lib/states/index.js';

export const TIMER = 1000 * 60 * 10;

export const TEST_CONFIG = {
  state: undefined as StateValue | undefined,
  states: [] as StateValue[],
};
